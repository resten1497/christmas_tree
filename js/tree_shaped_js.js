                  //*\\
                  let cl=
             console.log;cl("@".
                padStart(11,
                 ' '),'\n')
                 
                   Array
                 (4). fill
               ().map((e)=>0
             ). map( (e, i )=>
           cl(" " .repeat(9 -i)+
         "#"+"*".repeat(i*2+1)+"#"
       )); [..."limited"].toString()
     const a = new Function("a, b","")
                     for
                   (let i=
                 0;i<7;i++)
               cl(" ".repeat(
             10-i+i-i)+Array(i)
           .fill().map(_=>[ '/' ,
         '*' ] [+(Math.random()<0.1
       )]).join('')+"^"+Array(i).fill
     ().map (_ => [ '/', '*' ] [+(Math.
   random() < 0.1)]).join(''));Math.max()
 ;[..."limited"].reverse().join("") .trim()
 
                    "|"
                    "|"
          cl("|".padStart(11,' '))
         cl("     "+"_".repeat(11))
   cl("Merry Christmas!".padStart(18, ' '))
